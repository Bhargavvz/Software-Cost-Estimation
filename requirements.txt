# Core
# NOTE: torch is NOT listed here — NVIDIA servers have a pre-installed build.
# Only install manually if not present: pip install torch torchvision
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ML Baselines
scikit-learn>=1.3.0
xgboost>=2.0.0

# Data Quality
filterpy>=1.4.5          # Kalman filtering

# Explainability (install separately: pip install --no-deps shap)
# shap and captum have torch deps that conflict with NVIDIA pre-built torch

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
hashlib-additional>=1.0.1
